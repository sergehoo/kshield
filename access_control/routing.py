"""Routes WebSocket pour le flux temps réel des scans (Channels)."""
from django.urls import re_path

websocket_urlpatterns = [
    # ex: re_path(r"ws/dashboard/site/(?P<site_id>\d+)/$", consumers.SiteScansConsumer.as_asgi()),
]
